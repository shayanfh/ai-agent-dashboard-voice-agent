; Example only. Put custom FreePBX routing in extensions_custom.conf.
[from-internal-custom]
exten => 7000,1,NoOp(Route test call to LiveKit)
 same => n,Set(PJSIP_HEADER(add,X-Destination-Extension)=7000)
 same => n,Dial(PJSIP/+96824000000@livekit,60)
 same => n,Hangup()

