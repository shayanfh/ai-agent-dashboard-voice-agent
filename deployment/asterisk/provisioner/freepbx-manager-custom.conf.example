; Merge this section into /etc/asterisk/manager_custom.conf.
; Never replace existing FreePBX content and never expose AMI outside localhost.
[ai-provisioner]
secret=CHANGE_AMI_SECRET
deny=0.0.0.0/0.0.0.0
permit=127.0.0.1/255.255.255.255
read=system,command
write=system,command
writetimeout=5000
